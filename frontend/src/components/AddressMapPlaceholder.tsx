export function AddressMapPlaceholder() {
  return (
    <section className="panel">
      <h2>Address / map placeholder</h2>
      <p>
        Map rendering is intentionally omitted from the skeleton. This panel is reserved for
        approximate location display with address precision controls.
      </p>
    </section>
  );
}
